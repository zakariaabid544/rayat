# PostgreSQL (Render)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME

# JWT Secret (set a long random value before going live)
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Server Configuration
PORT=3000
NODE_ENV=production

# MQTT Configuration (Optional - for IoT devices)
MQTT_BROKER=mqtt://localhost:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# CORS Configuration
CORS_ORIGIN=https://your-domain.com
APP_BASE_URL=https://your-domain.com
PUBLIC_APP_URL=https://your-domain.com
PASSWORD_RESET_URL=https://your-domain.com/reset-password
PASSWORD_RESET_EXPIRY_MINUTES=45
PASSWORD_RESET_RATE_LIMIT_MAX=5

# SMTP Configuration (Password Reset)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=no-reply@your-domain.com
SMTP_PASS=CHANGE_ME
SMTP_FROM="Rayat Support <no-reply@your-domain.com>"

# Registration Email Notification
EMAIL_USER=notifications@your-domain.com
EMAIL_PASS=CHANGE_ME
EMAIL_TO=zakariaabid@hotmail.it

# Default Super Admin Credentials
ADMIN_DEFAULT_EMAIL=superadmin@your-domain.com
ADMIN_DEFAULT_PASSWORD=CHANGE_ME_TO_A_STRONG_ADMIN_PASSWORD
