{
  "name": "rayat-mqtt-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bridge MQTT -> HTTP per inoltrare i dati dei sensori verso rayat.ma",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node bridge.js",
    "dev": "node --watch bridge.js",
    "check": "node --check bridge.js"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.4.5",
    "mqtt": "^5.10.3"
  }
}
